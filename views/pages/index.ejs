<!DOCTYPE html>
<html>
<head>
    <%- include ("../partials/header") %>
</head>

<body>

<div class="container">
<h1>Database Results</h1>

<ul>
    <% results.forEach(function(r) { %>
        <li><%= r.id %> - <%= r.name %></li>
    <% }); %>
</ul>

</div>

</body>
</html>