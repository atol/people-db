<!DOCTYPE html>
<html>
<head>
    <%- include ("../partials/header") %>
</head>

<body>
    <%- include ("../partials/nav") %>
    <div class="container">
        <div class ="display">
            <h1>Profile</h1>

            <ul>
                <% results.forEach(function(r) { %>
                    <li><p>Name: <%= r.name.trim() %></p></li>
                    <li><p>Size: <%= r.size %></p></li>
                    <li><p>Height: <%= r.height %></p></li>
                    <li><p>Type: <%= r.type %></p></li>
                    <button id="edit" onclick="location.href='/user/<%= r.name.trim() %>/edit'">Edit</button>
                    <button id="delete" onclick="location.href='/user/<%= r.name.trim() %>/delete'">Delete</button>
                <% }); %>
            </ul>
        </div>
    </div>
</body>
</html>